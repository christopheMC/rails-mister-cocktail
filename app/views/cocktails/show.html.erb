<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "A la maison", cocktails_path, class: "btn btn-success" %>
      </li>
    </ul>
  </div>
  <div>
    <%= link_to "Ajouter des ingredients", new_cocktail_dose_path(@cocktail), class:"btn btn-success" %>
  </div>
</div>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
      <div class="card_show">
        <%= cl_image_tag @cocktail.photo.key, crop: :fill %>
        <h3>Ingrédients</h3>
        <ol>
          <% @cocktail.doses.each do |dose| %>
            <li style="color: $gray"><%= dose.ingredient.name %>: <%= dose.description %>
              <%= link_to dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } do %>
                <i class="fas fa-trash-alt"></i>
              <% end %>
            </li>
          <% end %>
        </ol>
      </div>
    </div>
  </div>
</div>
